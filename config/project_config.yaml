project_name: quant_integrated_project
language: en

paths:
  project_root: .
  data_dir: ./data
  raw_pdf_dir: ./data/raw_pdfs
  parsed_dir: ./data/parsed
  index_dir: ./artifacts/rag_index

artifacts:
  rag_index_dir: ./artifacts/rag_index
  retrieval_top_k: 5
  chunk_size: 1000
  chunk_overlap: 150

provider:
  primary: openai
  fallback: deepseek
